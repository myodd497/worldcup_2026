name: Daily ETL (08:00 UTC)

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

jobs:
  run-etl:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Poetry
        uses: abatilo/actions-poetry@v3
        with:
          poetry-version: "2.4.1"

      - name: Install dependencies
        run: |
          poetry config virtualenvs.in-project true
          poetry install --no-interaction --no-ansi

      - name: Configure environment
        env:
          BIGQUERY_PROJECT_ID: ${{ secrets.BIGQUERY_PROJECT_ID }}
          BIGQUERY_DATASET_ID: ${{ secrets.BIGQUERY_DATASET_ID }}
          API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
          GOOGLE_SERVICE_ACCOUNT_INFO: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_INFO }}
        run: |
          echo "BIGQUERY_PROJECT_ID=$BIGQUERY_PROJECT_ID" >> $GITHUB_ENV
          echo "BIGQUERY_DATASET_ID=$BIGQUERY_DATASET_ID" >> $GITHUB_ENV
          echo "API_FOOTBALL_KEY=$API_FOOTBALL_KEY" >> $GITHUB_ENV
          echo "GOOGLE_SERVICE_ACCOUNT_INFO=$GOOGLE_SERVICE_ACCOUNT_INFO" >> $GITHUB_ENV

      - name: Run full ETL
        run: poetry run python -m src.data.run_full_etl
